
# --- Auth (added by @7edge/auth-client) -----------------------------------
# Point this at the real API once it is available.
VITE_API_BASE_URL=http://localhost:3000/api

# `true` runs the library's in-memory mock backend (no network, no backend
# required). Set to `false` when VITE_API_BASE_URL is a live API.
VITE_AUTH_USE_MOCK=true
